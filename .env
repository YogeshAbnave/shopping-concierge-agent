# AWS Configuration
# Set these environment variables or configure via AWS CLI
# You can get these from AWS Console > IAM > Users > Security credentials
AWS_ACCESS_KEY_ID=[YOUR_ACCESS_KEY_ID]
AWS_SECRET_ACCESS_KEY=[YOUR_SECRET_ACCESS_KEY]
AWS_DEFAULT_REGION=us-east-1
AWS_REGION=us-east-1

# Deployment Configuration
DEPLOYMENT_ID=shopping-v3
GATEWAY_CLIENT_ID=4qc4qh6l3tu1sruijn1k1jq11o
GATEWAY_USER_POOL_ID=us-east-1_NKiFIQXjF
GATEWAY_SCOPE=concierge-gateway/invoke

# Optional: SerpAPI Key for product search
# Get this from https://serpapi.com/dashboard
SERP_API_KEY=[YOUR_SERP_API_KEY]

# Additional AWS Services (automatically configured via AWS CLI or above credentials)
# These are used by the application but don't need to be set manually:
# - DynamoDB tables (created by Amplify)
# - Bedrock model access (Claude)
# - SSM Parameter Store
# - API Gateway endpoints